# Repository Maintainability Index - Environment Configuration
# Copy this file to .env and fill in your actual values

# GitHub API Token
# Get your token from: https://github.com/settings/tokens
# Required scopes: public_repo, repo:status
# This token is used to increase GitHub API rate limits and access private repositories
GITHUB_TOKEN=your_github_personal_access_token_here

# OpenRouter API Key
# Get your key from: https://openrouter.ai/keys
# Required for LLM-powered analysis features (--llm flag)
# The application will work without this key, but LLM features will be disabled
OPENROUTER_API_KEY=sk-or-v1-your_openrouter_api_key_here

# LLM Model Configuration
# Default model for LLM analysis (optional)
# Examples: openai/gpt-4, openai/gpt-3.5-turbo, anthropic/claude-3-haiku
# If not set, uses: openai/gpt-oss-20b:free
OPENROUTER_MODEL=openai/gpt-oss-20b:free

# OpenRouter API Base URL (rarely needs to be changed)
# Default: https://openrouter.ai/api/v1
OPENAI_API_BASE=https://openrouter.ai/api/v1

# Setup Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys and tokens
# 3. Never commit .env to version control (it's already in .gitignore)
# 4. Use the application with: java -jar target/repo-maintainability-index-1.0.0.jar analyze owner/repo
#
# Alternative: Set environment variables directly in your shell:
# export GITHUB_TOKEN=your_token
# export OPENROUTER_API_KEY=your_key
# java -jar target/repo-maintainability-index-1.0.0.jar analyze owner/repo --llm